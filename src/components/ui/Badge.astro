---
export interface Props {
  label: string;
}

const { label } = Astro.props;
---

<span class="inline-flex items-center rounded-full border border-[color:var(--stroke)] bg-white/75 px-3 py-1 text-xs font-semibold text-[color:var(--brand-green)]">{label}</span>
